public class day46_gs {
    // 기본형 vs 참조형

    // 기본형 : 사용하는 값을 변수에 직접 넣는 것
    // 참조형 : 앞선 student처럼 객체가 저장된 메모리의 `주소`를 가르키는 참조값을 넣는 것

    // 기본형(Primitive Type) : int, long, double, boolean처럼 변수에 사용할 값을 직접 넣는 데이터 타입
    // 참조형(Reference Type) : 객체와 배열(메모리 주소를 찾아가야 함)

    // 계산(기본형은 그대로 계산, 참조형은 주소지에 직접 가야함)

    // 참고 - String은 사실 클래스다. 즉, 참조형이다. 그러나 매우 자주 다뤄지기 때문에 자바에서 특별하게 편의 기능을 제공한다
    
}
