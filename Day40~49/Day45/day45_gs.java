public class day45_gs {
    // 정리

    // 클래스가 필요한 이유 : 학생이라는 개념을 묶어서 만들고 싶다
    // 학생이라는 클래스를 만든 후 : 클래스에 속한 변수들을 `멤버` 혹은 `필드`라고 한다
    // 타입은 데잍터의 종류나 형태. int나 String처럼 Student라는 타입을 만들고 싶다
    // 이 설계도가 바로 클래스다
    // 클래스를 사용해 실제 메모리에 만들어진 실체를 객체 또는 인스턴스


    // 객체는 생성될 때마다 다른 참조값이 생긴다
    // 객체.필드 를 이용해 사용할 수 있다


    // 클래스 : 객체를 생성하기 위한 `틀`
    // 객체 : 실제로 존재하는 것
    // 인스턴스 : 특정 클래스로부터 생성된 객체(관계에 집중해서 이야기할 때)

    // 배열을 쓴다는 것은 메모리를 만들어 관리하겠다는 것
    // 배열은 곧 참조값을 저장해두는 것
}
